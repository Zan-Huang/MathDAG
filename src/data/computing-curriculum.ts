import type { Subtopic } from './math-curriculum'

const s = (id: string, title: string): Subtopic => ({ id, title })

export const computingCurriculum: Record<string, Subtopic[]> = {
  programming: [
    s('pg-syntax', 'Variables, types, expressions, and assignment in Python'),
    s('pg-control', 'Conditionals, loops, and early returns you can trace'),
    s('pg-fn', 'Functions, arguments, scope, and return values'),
    s('pg-data', 'Lists, dicts, sets, tuples, and when to use each'),
    s('pg-file', 'Read/write text and simple structured files (CSV, JSON)'),
    s('pg-err', 'Exceptions, reading a traceback, and defensive checks'),
    s('pg-mod', 'Modules, imports, and a script with a `__main__`'),
    s('pg-test', 'A function you can test with a few assertions'),
    s('pg-git', 'git add / commit / log / diff on a real project'),
    s('pg-cli', 'A command-line script with arguments, not only a notebook'),
    s('pg-debug', 'Print-free debugging: isolate, reproduce, bisect'),
  ],
  'scientific-python': [
    s('sp-array', 'NumPy arrays: shape, dtype, indexing, and views vs copies'),
    s('sp-bcast', 'Broadcasting and reductions (sum, mean, axis)'),
    s('sp-linalg', 'Array linear algebra: matmul, solve, eig, svd via the library'),
    s('sp-rand', 'Reproducible random numbers (generators and seeds)'),
    s('sp-scipy', 'SciPy: integrate.solve_ivp, optimize, and stats calls'),
    s('sp-plot', 'A labeled figure that states a claim (Matplotlib or similar)'),
    s('sp-table', 'Tabular data without mixing it up with tensors'),
    s('sp-repro', 'A script that runs from scratch with a pinned environment'),
    s('sp-conv', 'A discrete convolution or filter you wrote two ways'),
  ],
  algorithms: [
    s('al-asym', 'Worst-case time/space and what Big-O is not'),
    s('al-array', 'Arrays, lists, stacks, queues as layouts plus invariants'),
    s('al-hash', 'Hash tables: expected time and collision intuition'),
    s('al-heap', 'Heaps and priority queues; implement or simulate one'),
    s('al-tree', 'BSTs or a tree traversal you can write from memory'),
    s('al-sort', 'Sorting: comparison lower bound and when n log n is enough'),
    s('al-graph', 'BFS, DFS, and topological sort'),
    s('al-short', 'Dijkstra (and when negative edges break it)'),
    s('al-dp', 'Dynamic programming on a problem you recast as subproblems'),
    s('al-np', 'P vs NP as a claim about classes, not about your instance'),
  ],
  'scientific-computing': [
    s('sc-layout', 'A small package or repo: modules, tests, and a README'),
    s('sc-test', 'Numerical tests against a known solution or conservation law'),
    s('sc-sparse', 'Sparse matrices and why dense is a trap'),
    s('sc-profile', 'Profile before rewrite; name the hot loop'),
    s('sc-data', 'On-disk data (HDF5/NWB/Zarr or equivalent) vs ad-hoc folders'),
    s('sc-units', 'Units and dimensionless numbers as executable checks'),
    s('sc-config', 'Parameters in files, not magic numbers in cells'),
    s('sc-log', 'A computational experiment you can rerun from a seed'),
    s('sc-sim', 'A simulator (ODE/network/PDE) with a verification plot'),
  ],
  'gpu-hpc': [
    s('gpu-hier', 'Memory hierarchy: registers, SRAM, HBM, and why movement wins'),
    s('gpu-bound', 'Roofline: compute-bound vs memory-bound on a real kernel'),
    s('gpu-dev', 'Device vs host in PyTorch or JAX; no silent copies'),
    s('gpu-batch', 'Batch size as both a systems and a statistical choice'),
    s('gpu-prec', 'Mixed precision as a numerical-methods decision'),
    s('gpu-par', 'Data parallel vs model parallel as different answers'),
    s('gpu-prof', 'Profile a training or simulation step and read the trace'),
    s('gpu-kernel', 'One custom kernel or fused op you understand (Triton/CUDA/library)'),
    s('gpu-dist', 'A first multi-GPU or distributed data-parallel run, even tiny'),
  ],
}
